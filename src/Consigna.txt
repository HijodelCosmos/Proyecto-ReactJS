

Por Agregar:
    vision de orden en tramite
    contex order (X)
    form user
    Loaders
    Markdowns
    .env

Manejar stocks (ver clase firebase II min: 1:58:00)cart linea 44 (por ahora si no busca dentro de updateStock)

>> Consigna: Conecta Firestore a tu APP para escrituras.

>>Aspectos a incluir en el entregable:

Utiliza las operaciones de inserción para insertar tu orden en la colección y dale al user su id de orden auto-generada
Crea los mappings para poder grabar un objeto del formato { buyer: { name, phone, email }, items: [{id, title, price}], date, total  }

Tips
    CONTROL F2 MULTICURSOR